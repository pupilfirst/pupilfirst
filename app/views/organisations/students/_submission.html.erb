<a href="<%= timeline_event_path(submission) %>" class="block p-5 mt-4 border border-gray-200 bg-white rounded-lg hover:bg-primary-50 hover:text-primary-500 hover:border-primary-500 cursor-pointer">
  <div class="flex justify-between items-center gap-4">
    <div>
      <p class="font-semibold">
        <%= submission.target.title %>
      </p>
      <% if submission.evaluated_at.present? %>
        <span><%= submission.passed_at.present? ? t('.accepted') : t('.rejected') %></span>
        |
        <span title="<%= submission.evaluated_at.iso8601 %>"><%= t('.reviewed_time_ago', time_ago_in_words: time_ago_in_words(submission.evaluated_at)) %></span>
      <% end %>
      <p class="text-sm text-gray-600">
        <span title="<%= submission.created_at.iso8601 %>"><%= t('.submitted_time_ago', time_ago_in_words: time_ago_in_words(submission.created_at)) %></span>
      </p>
    </div>
    <div class="flex flex-col items-center py-2 px-2 text-xs bg-yellow-50 text-yellow-900 border border-yellow-900 rounded-lg">
      <p><%= t('shared.level') %></p>
      <p class="font-bold"><%= submission.target.level.number %></p>
    </div>
  </div>
</a>
