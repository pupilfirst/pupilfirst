<% link = admins_school_url %>

<% content_for :salutation do %>
  Hello <%= @school_admin.name %>,
<% end %>

<% content_for :body do %>
  <p>
    A new administrator was added to your school <strong><%= @school.name %></strong>, on
    <%= @new_school_admin.created_at.strftime('%B %-d, %Y at %l:%M %p') %> by <%= @adding_user.name %>.
  </p>

  <p>
    The new admin's name is <strong><%= @new_school_admin.name %></strong>, and has the registered email address
    <a href="mailto:<%= @new_school_admin.email %>"><%= @new_school_admin.email %></a>.
  </p>

  <p>
    If you do not recognize this user, you can can remove them from the school administration interface using this <%= link_to 'link', link%>.
  </p>

  <%= render 'mailers/link_fallback', url: link %>
<% end %>
