.resource-tags.m-y-1 class="#{fade ? 'resource-tags-fade' : ''}"
  - resource.tags.each do |tag|
    = link_to resources_path('resources_filter[tags][]' => tag.name), class: 'tag tag-pill tag-default' do
      = tag.name
