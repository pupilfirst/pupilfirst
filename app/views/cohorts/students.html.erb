<% content_for(:head) do %>
  <title><%= @presenter.page_title %></title>
<% end %>
<div class="pb-20 md:pb-10 h-full md:h-screen overflow-y-auto">
  <%= render 'shared/students', overview_path: cohort_path(@cohort) %>
</div>
