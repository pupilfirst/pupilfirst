deployment:
    production:
      branch: master
      commands:
        - git push git@heroku.com:svapp.git $CIRCLE_SHA1:master
        - heroku run rake db:migrate --app svapp
        - heroku restart --app svapp
    staging:
      branch: development
      commands:
        - git push git@heroku.com:svapp-staging.git $CIRCLE_SHA1:master
        - heroku run rake db:migrate --app svapp-staging
        - heroku restart --app svapp-staging
