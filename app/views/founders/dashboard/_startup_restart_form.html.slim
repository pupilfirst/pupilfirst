#startup-restart-form.modal.fade
  .modal-dialog
    .modal-content
      .modal-header
        | Pivot your startup journey!
        button.close.modal-close aria-label="Close" data-dismiss="modal" type="button"
          span aria-hidden="true"
            | &times;
      .modal-body
        - if @startup.requested_restart_level.present?
          .alert.alert-danger role="alert"
            | Your startup has already submitted a pivot request! Please wait for approval.
        - elsif @startup.restartable_levels.present?
          .simple-form-container
            = simple_form_for @restart_form, url: startup_restart_founder_path, method: :post do |f|
              = f.input :level_id, label: 'Pick a level to return to:',
                collection: current_founder.startup.restartable_levels,
                include_blank: false
              = f.input :reason, label: 'Why do you want to pivot?',
                input_html: { maxlength: 300, rows: 6 },
                as: :text

              = button_tag type: :submit, class: 'btn btn-primary text-uppercase' do
                | Request For a Pivot
        - else
          .alert.alert-warning role="alert"
            | Too soon! You need to be at level 2 or above to pivot.
