<p>Welcome <%= @resource.fullname.present? ? @resource.fullname : @resource.email %>!</p>

<p>Thank you for signing up with SVLabs. To help us serve you better, kindly confirm your email through the link below:</p>

<p><%= link_to 'Confirm my account', confirmation_url(@resource, confirmation_token: @token) %></p>

<p> If the above link does not work for you, please copy and paste the following into your browser's address bar:</p>

<pre> #{confirmation_url} </pre>