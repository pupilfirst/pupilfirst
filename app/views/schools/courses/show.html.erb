<%= form_with(model: @course, url: school_course_path(@course)) do |f| %>
  <%= f.text_field :name, required: true, class: 'border' %>
  <%= f.submit 'Update', class: 'underline cursor-pointer' %>
<% end %>

Overview about the course '<%= @course.name %>' should be shown here.
