- presenter = Founders::FeeOfferPresenter.new(self, current_startup, period)
.col-sm-4.content-box.text-center.fee-offer__box class=(period == 6 ? 'fee-offer__recommended' : '')
  - if period == 6
    .fee-offer__recommended-notice
      | Recommended!

  h5.font-semibold.text-uppercase.fee-offer_period.pb-2.mb-3 #{presenter.period_string}

  .my-4
    .fee-offer__amount-highlight
      - if presenter.discount_applicable?
        h5.font-semibold.mb-0
          ' <del>&#8377;#{presenter.undiscounted_fee}</del>
      h2.font-semibold.mb-0
        span.font-regular &#8377;
        ' #{presenter.fee_payable}
      p
        | for #{current_startup.billing_founders_count} founders
      .fee-offer__discount-details
        - if presenter.discount_applicable?
          h6.fee-offer__discount-title DISCOUNT APPLIED
          p
            - if period != 1
              | #{presenter.bulk_package_discount}% off (Long-term Plan)
              br
            - if presenter.coupon_discount.present?
              | #{presenter.coupon_discount}% off (Discount Coupon)
              br
            span.dark-secondary You save &#8377;#{presenter.discount_amount}!
        - else
          h6.fee-offer__discount-title FULL PRICE

  = form_with url: fee_founder_path, method: :post, class: 'js-founder-fee__form' do |form|
    .px-4
      = hidden_field_tag 'fee[period]', period

      .js-founder-fee__pay-button
        = form.button class: "btn btn-md text-uppercase btn-with-icon #{(period == 6 ? 'btn-primary' : 'btn-ghost-primary')}" do
          | Pay for #{presenter.period_string}

  .js-founder-fee__disabled-pay-button.d-none
    .px-4
      button class="btn btn-primary btn-md text-uppercase btn-with-icon disabled"
        i.fa.fa-spinner.fa-pulse
        | Please wait...

  .fee-offer__error.brand-danger.mt-2.d-none
    i.fa.fa-warning
    .font-semibold
      ' Something went wrong!
    | Please refresh the page and try again
